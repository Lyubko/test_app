<!-- Validation failure messages -->
<% if @cast.errors.any? %>
  <% @cast.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Cast</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_cast" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for movie_id -->
      <div class="form-group">
        <label for="movie_id" class="control-label">
          Movie
        </label>

        <%= select_tag(:movie_id, options_from_collection_for_select(Movie.all, :id, :movie_name, @cast.movie_id), :class => "form-control") %>
      </div>

      <!-- Label and input for actor_id -->
      <div class="form-group">
        <label for="actor_id" class="control-label">
          Actor
        </label>

        <input type="text" id="actor_id" name="actor_id" placeholder="Actor" class="form-control" value="<%= @cast.actor_id %>">
      </div>

      <button class="btn btn-success">
        Create Cast
      </button>
      or
      <a href="/casts">Cancel</a>
    </form>
  </div>
</div>
